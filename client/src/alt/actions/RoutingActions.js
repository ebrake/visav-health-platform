import alt from '../alt'
import responseUtil from '../../components/utils/responseUtil';

class RoutingActions {
  setAfterLoginRoute(nextState) {
    return nextState;
  }
}

export default alt.createActions(RoutingActions);